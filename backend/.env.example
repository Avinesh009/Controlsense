# Copy this file to `.env` in the same folder and fill in real values.
# NEVER commit the real `.env` file — it's already covered by .gitignore.

# ---- Database (optional; falls back to in-memory storage if unset) ----
SUPABASE_URL=https://rdkkisoybyjkjliseksz.supabase.co
SUPABASE_KEY=sb_publishable_Fe_QIUR9VClzDrfm49dPuQ_pcNZDyd0

# ---- Telemetry ingestion (shared secret between backend and desktop agent) ----
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
# Must match the `api_secret_key` value in desktop_agent/config.json
API_SECRET_KEY=2f33cc7c8a2cc67f7e447ed653c765eea4b0c38190a2de6b392456d31a92712d


# ---- Admin dashboard login ----
# Generate the hash with: python scripts/generate_password_hash.py
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=$2b$12$UUS9p3VjL6fVofCwqHH0SOb/MFvbJlZ8LaqjGtEdcGI7OWl5fJv9u

# Secret used to sign admin JWTs. Generate with:
# python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=2f33cc7c8a2cc67f7e447ed653c765eea4b0c38190a2de6b392456d31a92712d

# Optional: minutes before an admin session expires (default 480 = 8 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=480

# ---- CORS ----
# Comma-separated list of origins allowed to call this API (your dashboard URL(s))
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

